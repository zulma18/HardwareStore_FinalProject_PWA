@model HardwareStore.Models.Logins_Model

@{
    ViewData["Title"] = "Edit";
    ViewData["TitlePage"] = "Actualizar datos del administrador";
}

<section class="bg-white py-3 px-4 container rounded-2" style="max-width:800px;">
    <h4 class="pt-2">Datos del Producto</h4>
    <hr />
    <div class="row px-md-3">
        <div class="col-md-12">
            <form asp-action="Edit" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <input type="hidden" asp-for="LoginId" />

                <div class="form-group">
                    <label asp-for="LoginUser" class="control-label"></label>
                    <input asp-for="LoginUser" class="form-control" />
                    <span asp-validation-for="LoginUser" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Id_Rol" class="control-label"></label>
                    <select asp-items="@ViewBag.Rol" asp-for="Id_Rol" class="form-select">
                        <option value="">Seleccione</option>
                    </select>
                    <span asp-validation-for="Id_Rol" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <button type="submit" class="btn btn-warning">Editar <i class="lni lni-pencil-alt"></i></button>
                    <a class="btn btn-primary" asp-action="Index">Volver a la lista <i class="lni lni-arrow-left"></i></a>
                </div>
            </form>
        </div>
    </div>
</section>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

