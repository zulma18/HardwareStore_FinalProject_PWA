@model HardwareStore.Models.Logins_Model

@{
    ViewData["Title"] = "Delete";
    ViewData["TitlePage"] = "Eliminar administrador";
}

<section class="bg-white py-3 px-4 container rounded-2" style="max-width:1000px;">
    <h3>¿Estas seguro de eliminar este administrador?</h3>
    <div>
        <hr />
        <dl class="row">

            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.LoginUser)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.LoginUser)
            </dd>
            <dt class="col-sm-2">
                Nombre del rol
            </dt>
            <dd class="col-sm-10">
                @ViewBag.Rol
            </dd>
        </dl>

        <form asp-action="Delete">
            <button type="submit" class="btn btn-danger">Eliminar <i class="lni lni-trash-can"></i></button>
            <a class="btn btn-primary" asp-action="Index">Volver a la lista <i class="lni lni-arrow-left"></i></a>
        </form>
    </div>

</section>



